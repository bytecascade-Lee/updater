# 版本发布说明

如有问题或建议，欢迎在 [Issue](https://github.com/bytecascade-Lee/updater/issues) 区提出。

---

## 0.1.2

### 版本亮点

统一了构建信息与自动更新清单的时间格式，全部采用 ISO 8601 标准，便于解析和对比。

### 体验改进

- 构建信息（`--build-info`）和自动更新清单（`latest.json`）的时间字段统一为 ISO 8601 标准格式，不再使用毫秒时间戳
- 新增 `build-info-example.json` 示例文件，方便集成方参考字段结构

### 重要修复

暂无。

### 升级注意

自动更新清单 `schemaVersion` 从字符串改为数字，`publishTimestamp` 改名为 `publishDate`。如有自定义客户端解析 `latest-*.json`，需同步适配。

### 已知问题

暂无。

---

## 0.1.1

### 版本亮点

改善了 CLI 命令在不同启动方式下的输出可见性，并增强了日志和配置模块的健壮性。

### 体验改进

- 优化了命令行输出：双击运行或通过脚本调用时，`-v`、`-h`、`--build-info` 等命令的输出现在能正确显示，不再因窗口一闪而过而无法阅读
- 日志文件所在目录现在会自动创建，配置中指定的日志路径不存在时不再报错

### 重要修复

暂无。

### 升级注意

暂无。

### 已知问题

暂无。

---

## 0.1.0

### 版本亮点

首个正式版本。`updater` 是一个独立的外部更新器，专门为 Tauri 便携版（portable zip）应用提供自动更新能力——弥补官方更新插件仅支持安装包、不支持便携版的不足。

### 新增功能

- **完整更新流程**：支持等待旧进程退出 → 替换文件 → 启动新进程的完整管道，开箱即用
- **备份与回滚**：更新前自动备份，新版本启动失败时可一键还原旧版本，安全无忧
- **灵活启动模式**：支持分离启动、守护指定时长、无限驻留三种模式，适配不同应用场景
- **配置驱动**：通过 JSON 配置文件控制所有行为，包含等待策略、更新规则、启动参数、回滚策略等完整字段
- **保留用户数据**：更新时可指定保留特定文件和目录，用户数据不丢失
- **双架构支持**：同时支持 x86_64 和 arm64 两种 Windows 架构
- **自动更新清单**：发布时自动生成 latest.json，包含各架构的下载地址、SHA256 校验值和文件大小
- **双平台发布**：每次发布同时发布到 GitHub Release 和 CNB Release

### 体验改进

- 更新过程中实时显示进度信息（等待、备份、清理、拷贝、启动各阶段）
- 无头模式支持，可作为后台服务静默运行
- 日志自动记录到文件，方便问题排查

### 重要修复

暂无。

### 升级注意

暂无。这是首个版本，无历史版本需要迁移。

### 已知问题

暂无。
